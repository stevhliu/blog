# Implementation Plans

Generated by the improve skill on 2026-07-15, against commit `4e137d21`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Repo verification gate: `pnpm verify` (typecheck + lint + test + build).
Every plan assumes it and must leave it green.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix image alt/caption parsing | P1 | S | — | TODO |
| 002 | Surface post descriptions (meta + feed) | P1 | S | — | TODO |
| 003 | Diagram render-test harness | P1 | M | — | TODO |
| 004 | Harden tweet pipeline (cache-first, types, tests) | P1 | M | — | TODO |
| 005 | View API route tests | P2 | S | — | TODO |
| 006 | ESLint 9 flat config migration | P2 | M | — | TODO |
| 007 | Shiki fine-grained bundle | P2 | S | — | TODO |
| 008 | Hygiene sweep (playwright, dead CSS, README, discarded query) | P2 | S | — (coordinates with 002) | TODO |
| 009 | Collapse diagram registry boilerplate | P3 | S | 003 (recommended) | TODO |
| 011 | Rate-limit view API | P3 | S–M | 005 | TODO |
| 010 | Dedupe attention/quantization SVG scaffolding | P3 | M | 003 (hard) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **010 and 014 require 003**: the render harness (and 010's snapshots built
  on it) are the only safety net for refactoring untested visual components.
- **011 requires 005**: the rate-limit tests extend 005's test file and mock
  scaffolding.
- **008 coordinates with 002**: both touch the README's "Adding a post"
  section; 008's Step 3 contains wording for either ordering.
- **009 before 012 is convenient, not required**: the gallery reads the
  registry either way; 009's loader map is where family metadata could later
  come from.
- **013 interacts with 002/008** only through README wording (one sentence).
- Plans 001–008 are mutually independent and safe to run in parallel
  worktrees, except the 002/008 README coordination above.

## Editorial notes (not plans)

- The draft post `how-transformers-standardize-models` (dated July 25, 2026)
  is essentially finished — publishing it is an editorial decision, optionally
  automated by plan 013's date gate.
- Whether to link the diagram gallery (plan 012) from site navigation is left
  to the maintainer once the page exists.

## Findings considered and rejected

Recorded so future audits don't re-litigate them:

- **`dynamic()` wrappers in `mdx-diagrams.ts` as a bundle cost** — refuted
  against build output: diagrams are server-only chunks, zero client JS
  regardless of the shared registry. Plan 009 addresses the *boilerplate*,
  not a bundle problem.
- **Atom feed throwing on an unparseable date** — by design (fail-fast);
  `tests/atom.test.ts` asserts the throw and `pnpm verify` catches bad dates
  before deploy.
- **`scripts/assemble-gif.py`** — manual authoring utility, intentionally
  unwired; not dead code worth removing.
- **Committing `AGENTS.md`/`CLAUDE.md`** — deliberately gitignored by the
  maintainer; respected.
- **Tailwind v4 migration** — supported v3, small site, ~40 hand-tuned SVG
  components at pixel risk; not worth it now.
- **Prettier 3 bump** — cosmetic, whole-repo reformat cost; do opportunistically
  or never.
- **Stored-XSS via tweet cache** — LOW confidence; react-tweet escapes text
  and cache writes require the server key. Revisit only if react-tweet's URL
  handling changes (noted in plan 004's maintenance notes).
- **Tags/search pages** — no tag data exists anywhere in the repo; full-text
  search over 7 posts isn't a real need. No repo signal.
- **Per-heading OG images** — nothing in the OG code or posts points at
  heading-level sharing.
