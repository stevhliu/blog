# Supabase project URL (public). Optional — without it view counts are disabled.
NEXT_PUBLIC_SUPABASE_URL=
# Supabase secret (service) key. Server-only; never exposed to the client.
SUPABASE_SECRET_KEY=
